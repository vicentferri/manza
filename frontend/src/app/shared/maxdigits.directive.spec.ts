import { MaxdigitsDirective } from './maxdigits.directive';

describe('MaxdigitsDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxdigitsDirective();
    expect(directive).toBeTruthy();
  });
});
